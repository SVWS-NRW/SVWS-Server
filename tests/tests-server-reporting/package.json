{
	"name": "@svws-nrw/svws-vite-reporting-test",
	"description": "SVWS-Server-Projekt: Reporting-Snapshot-Tests",
	"author": "MSB NRW",
	"license": "BSD-3-Clause",
	"version": "1.5.0-SNAPSHOT",
	"type": "module",
	"scripts": {
		"test": "vitest run ./tests",
		"test:local": "vitest run ./tests",
		"test:tempDockerEnv": "vitest run ./tests --coverage.reporter=html --mode allowDestructiveTests",
		"lint": "eslint \"tests/**/*.{ts,vue}\" --cache --max-warnings=0",
		"build": "tsc"
	}
}
