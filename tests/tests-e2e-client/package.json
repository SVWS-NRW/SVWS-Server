{
	"name": "svws-test",
	"version": "1.0.6-SNAPSHOT",
	"description": "",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"installdeps": "playwright install-deps",
		"install": "playwright install",
		"test:local" : "playwright test",
		"test" : "playwright test",
		"show": "playwright show-report",
		"lint": "eslint 'src_old/**/*.ts' --cache",
		"codegen": "playwright codegen https://localhost -o build/codegen/gen_test.ts --ignore-https-errors"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@playwright/test": "^1.50.0"
	}
}
