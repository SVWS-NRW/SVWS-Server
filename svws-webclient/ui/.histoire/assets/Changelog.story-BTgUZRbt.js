import{c as t,a as d,o as i}from"./index-Ypxb2S-y.js";const o={class:"prose"},u="design-system",s="Changelog",h="#ccc",b="changelog",g="carbon:bookmark",m={__name:"Changelog.story",setup(r,{expose:n}){return n({frontmatter:{group:"design-system",title:"Changelog",iconColor:"#ccc",id:"changelog",icon:"carbon:bookmark"}}),(a,e)=>(i(),t("div",o,e[0]||(e[0]=[d('<h1>Changelog</h1><p>Chronologische Auflistung von Änderungen an den Komponenten Fix/Feature/Breaking Change:</p><h2>ab 1.0.2</h2><p>Modale verwenden nun zum Öffnen mit dem <code>:show</code>-Prop lediglich ein boolean statt der bisherigen Funktion</p><h2>ab 1.0.1</h2><ul><li><a href="/story/farben">Design Tokens</a> werden eingeführt. Deprecated Farbdefinitionen sollen nicht mehr verwendet werden. In den Docs sind zu dem Thema alle wichtigen Details nachzulesen und es gibt eine Übersicht aller verfügbaren Variablen.<br> Beispiel: <code>bg-white dark:bg-black text-black dark:text-white hover:text-primary dark:hover:text-primary-dark</code> → <code>bg-ui text-ui hover:text-ui-hover</code></li><li>Dark Mode wird ab jetzt automatisch über die semantischen Design Tokens umgesetzt, alle <code>dark:bg-color</code> classes entfallen.</li></ul><h2>ab 0.9.3</h2><ul><li>Die Tabelle bekommt ein neues Prop <code>unselectable</code>. Hier wird als Set eine Liste aller Items übergeben, die sich nicht auswählen lassen, z.B. Vorgaben.</li><li>ebenfalls gibt es neu <code>allowArrowKeySelection</code>. Das erlaubt die Auswahl der Items per Tastatur.</li><li>Die Checkbox stellt nun per <code>defineExposed</code> auch den <code>content</code>und das <code>input</code> zur Verfügung, analog zum TextInput.</li></ul><h2>ab 0.9.2</h2><ul><li>Das Text-Input unterstützt keinen Typ number mehr und verlangt als Input den Typ string | null. Für Zahleneingabefelder ist als Ersatz der Number-Input zu nehmen.</li></ul><h2>ab 0.8.16</h2><ul><li>Das Multiselect bekommt die Prop <code>autofocus</code>. Wird das übergeben, dann öffnet sich automatisch das Auswahlmenü. Wird z.B. bei Stundenplan-Unterrichte verwendet</li><li>Das MS sendet auch ein <code>blur</code>-Event, damit man beim Verlassen z.B. auch das MS wieder entfernen kann. Ebenso bei den Unterrichten zu finden.</li></ul><h2>ab 0.8.11</h2><ul><li>Die Table-Komponente kennt nun auch zwei weitere Slots: <code>row</code> und <code>rowSelectable</code> und <code>rowCustom</code>. Dies gibt nun die jeweiligen Rows zurück und sollte statt <code>body</code> verwendet werden. ** <code>row</code> ist bereits in ein <code>tr</code> gewrappt und liefert nur die <code>td</code>. ** <code>rowSelectable</code> hat zusätzlich als erstes <code>td</code> eine Checkbox für die Zeilenauswahl. ** <code>rowCustom</code> hat kein <code>tr</code> und muss vollständig implementiert werden.</li><li>Man kann der Tabelle ein Set <code>hiddenColumns</code> üergeben mit den Keys der Spalten, um festzulegen, welche Spalten standardmäßig ausgeblendet sind. Kann auch als <code>v-model:hiddenColumns</code> verwendet werden, um z.b. die Daten anderweitig zu speichern.</li><li>Das Select kann nun ein Vorgabe-Item hervorheben. Wird z.B. bei dem aktuellen Schulabschnitt genutzt. <code>:highlightItem</code></li></ul><h2>ab 0.8.9</h2><ul><li>Die Checkbox hat das Attribut <code>color = &#39;success&#39; | &#39;error&#39; | &#39;warning&#39;;</code> dazubekommen.</li></ul><h2>ab 0.7.8</h2><ul><li>Die Checkbox verwendet nun das Attribut <code>indeterminate</code>, um den Zustzand von <code>undefined</code> oder <code>null</code>, unabhängig vom verwendeten <code>v-model</code> bzw. <code>model-value</code> anzuzeigen. <code>model-value</code> muss nun ein Boolean-Wert sein. Dazu wurde ein <code>readonly</code> Attribut hinzugefügt, das Eingaben verhindert.</li><li>Die Tabelle hat zwei neue Attribute bekommen: <code>sortByAndOrder?: {key: string|null; order: boolean|null}</code> und <code>sortByMulti?: Map&lt;string, (boolean | null)&gt;</code>. <code>sortByAndOrder</code> gibt an, welcher key in der Tabelle sortiert wird aktuell und ob es <code>true</code>-&gt; aufsteigend, <code>false</code>: absteigend oder <code>null</code> -&gt; nicht sortiert wird. <code>sortByMulti</code> übergibt diese Information als Map, wenn nach mehreren Keys sortiert wird. Damit können mehrere Felder gleichzeitig sortiert werden. Allerdings muss dafür die Sortierung extern vorgenommen werden und <code>items</code> entsprechend der Sortierung aktualisiert werden. Dazu gibt es ein Event, was über den Stand der aktuellen Auswahl informiert: <code>@update:sort-by-and-order</code>. Je nach <code>order</code> können entsprechende Filter dann zu- oder abgeschaltet werden. Die beiden Props <code>sortBy</code> und <code>sortingOrder</code> wurden entfernt.</li><li>Es wurde eine neue Input-Komponente <code>SvwsUiInputNumber</code> hinzugefügt. Diese übernimmt die Aufgabe des Text-Inputs für den Type <code>number</code> und stellt nun typ-sicher Zahlen oder null zur Verfügung. In zukünfigen Versionen soll der type <code>number</code> bei dem Text-Input entfernt werden.</li></ul><h2>ab 0.7.3</h2><ul><li>Modal wurde überarbeitet und stellt nicht mehr die Funktionen openModal, closeModal und isOpen zur Verfügung. Das Öffnen erfolgt nun über die Property show, mit der eine Funktion übergeben wird, welche eine vue-Ref auf einen boolean zurückgibt</li><li>Das MultiSelect kann nur noch die Variante mit Tags übernehmen, ohne Tags wird mit Select verwendet. Da sich bis auf das <code>tags</code>-Prop nichts ändert, kann mit Suchen und Ersetzen diese Komponente relativ problemlos ausgetauscht werden.</li></ul><h2>ab 0.7</h2><ul><li>DataTable ist nun auf generics umgestellt, die Items bestimmen den Typ.</li><li>Multiselect, TextInput und TextArea geben nun auch den Wert bei @blur/@change raus. Darüber hinaus verwalten sie nun den internen State.</li></ul><h2>ab 0.6.18:</h2><ul><li>TextInput: Fix, wenn <code>type=&quot;number&quot;</code> ist, dann wird beim Klicken der rauf/runter-Pfeile kein <code>emit</code> ausgelöst, weil <code>document.ativeElement</code> nicht das Input-Feld ist. Es muss erst durch ein <code>focus()</code> gesetzt werden.</li><li>Multiselect kann bei autocomplete nun auch per Tab das erste Element auswählen.</li><li>emits werden bei TextInput und TextArea nur noch bei <code>blur</code>-Events ausgegeben.</li><li>Drag und Drop wird entfernt</li></ul>',24)])))}};export{m as default,u as group,g as icon,h as iconColor,b as id,s as title};
