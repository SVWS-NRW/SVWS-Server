package de.svws_nrw.repo.enm;

import de.svws_nrw.db.dto.current.svws.timestamps.DTOTimestampsNotenmodulCredentials;
import de.svws_nrw.repo.Repository;

/**
 * Das Interface für ein Repository zum Zugriff auf die Tabelle mit den Zeitstempel für die Lehrer-Credentials des Notenmoduls der SVWS-Datenbank
 */
public interface NotenmodulCredentialsTimestampsRepository extends Repository<DTOTimestampsNotenmodulCredentials> {

	/* Hier können bei Bedarf weitere Methoden ergänzt werden. */

}
